import 'package:flutter/material.dart';

class filtered_screen extends StatefulWidget {
  const filtered_screen({super.key});

  @override
  State<filtered_screen> createState() => _filtered_screenState();
}

class _filtered_screenState extends State<filtered_screen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
